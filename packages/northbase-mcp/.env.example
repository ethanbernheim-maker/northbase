# northbase-mcp uses the same session as the northbase CLI.
# Run `northbase login` first to authenticate.
# Session is stored at ~/.northbase/session.json and shared automatically.
#
# Optional debug flag:
# NORTHBASE_DEBUG=1
